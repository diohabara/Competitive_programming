#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
#define ll long long
#define endl '\n'
using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    ll n;
    cin >> n;
    string s;
    if (n == 0) {
        s = "0";
        cout << s << endl;
    } else {
        if (n % 2 == 1) {
            n--;
            s = "1";
        } else {
            
        }
    }
    return 0;
}