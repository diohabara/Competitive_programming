#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cstdio>
#include <cmath>
#include <string>
#include <vector>
#include <set>
#include <stack>
#include <functional>
#define rep(i, n) for(int i = 0; i < n; i++)
#define ll long long
using namespace std;

int sum[40000];

int main()
{
	cin.tie(0);
	ios::sync_with_stdio(false);
    // input
    int n;
    int a[15];
    cin >> n;
    rep(i, n) {
        cin >> a[i];
    }

    int index = 0;
    rep(i, n) {

    }
	return 0;
}