#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
#define ll long long
#define endl '\n'
using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    int n, k;
    cin >> n >> k;
    int x[100000];
    for (int i = 0; i < n; i++) {
        cin >> x[i];
    }
    ll sum = 0;
    sort(x, x + n);
    for (int i = 0; i < k; i++) {

    } return 0;
}