#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
#define ll long long
#define endl '\n'
using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    int a, b, q;
    cin >> a >> b >> q;
    ll s[100000], t[100000], x[100000];
    rep(i, a) { cin >> s[i]; }
    rep(i, b) { cin >> t[i]; }
    rep(i, q) { cin >> x[i]; }
    return 0;
}